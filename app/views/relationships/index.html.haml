%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @relationships.each do |r|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(r.followed.email.downcase)}?s=40")
            = link_to r.followed.full_name, user_path(r.followed)
          %td.extra-padding= r.followed.queue_items.count
          %td.extra-padding= r.followed.followers.count
          %td.extra-padding
            = link_to r, method: :delete do
              %i.glyphicon.glyphicon-remove
