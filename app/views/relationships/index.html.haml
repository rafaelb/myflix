%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - current_user.followed_users.each do |f|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(f.email.downcase)}?s=40")
            = link_to f.full_name, user_path(f)
          %td.extra-padding= f.queue_items.count
          %td.extra-padding= f.followers.count
          %td.extra-padding
            = link_to "", method: :post do
              %i.glyphicon.glyphicon-remove
